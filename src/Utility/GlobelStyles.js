import {StyleSheet} from 'react-native';
import {colors} from './Colors';

export const globelstyle = StyleSheet.create({
  errorText: {
    color: colors.primary,
    fontSize: 12,
    fontWeight: '700',
    marginTop:-8,
  },
});
